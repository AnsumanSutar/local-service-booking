{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx prisma generate --schema=prisma/schema.prisma && npx prisma db push --accept-data-loss --schema=prisma/schema.prisma && node index.js",
    "build": "npx prisma generate --schema=prisma/schema.prisma && npx prisma db push --accept-data-loss --schema=prisma/schema.prisma",
    "postinstall": "npx prisma generate --schema=prisma/schema.prisma",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "6.4.1",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "multer": "^2.1.0",
    "prisma": "6.4.1"
  }
}